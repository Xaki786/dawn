{{ 'component-image-with-text.css' | asset_url | stylesheet_tag }}
<h1>{{ section.settings.title }}</h1>

{% schema %}
{
    "name": "Test Section",
    "tag": "section",
    "class": "slideshow",
    "settings": [
      {
        "type": "text",
        "id": "title",
        "label": "Title"
      }
    ]    
}
{% endschema %}